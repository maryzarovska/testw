const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "admin",
        database: "funtest",
    },
    listPerPage: 10,
    jwtSecret: 'TOP_SECRET',
};

module.exports = config;